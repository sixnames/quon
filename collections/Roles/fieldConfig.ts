import { commonFieldConfig } from '@/collections/coomonFieldConfig';

export const roleFieldConfig = {
  create: commonFieldConfig.create,
  read: commonFieldConfig.read,
  update: commonFieldConfig.update,
  delete: commonFieldConfig.delete,
  generate: commonFieldConfig.generate,
  label: commonFieldConfig.label,
};
